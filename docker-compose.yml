services:
  saturnalia-docs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: saturnalia-2025-docs
    ports:
      - "4377:4377"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # nginx will serve the built static site from /usr/share/nginx/html